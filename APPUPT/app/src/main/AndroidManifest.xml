<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para usar internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CREDENCIALES"
        tools:targetApi="31">

        <!-- Pantalla principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>

        </activity>

        <!-- ESTUDIANTE -->

        <activity
            android:name=".LoginEstudianteActivity"
            android:exported="false"/>

        <activity
            android:name=".RegistroCuentaActivity"
            android:exported="false"/>

        <activity
            android:name=".FormularioCredencialActivity"
            android:exported="false"/>

        <activity
            android:name=".EstadoSolicitudActivity"
            android:exported="false"/>


        <!-- ADMINISTRADOR -->

        <activity
            android:name=".LoginAdminActivity"
            android:exported="false"/>

        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false"/>

        <activity
            android:name=".DetalleAlumnoActivity"
            android:exported="false"/>

    </application>

</manifest>